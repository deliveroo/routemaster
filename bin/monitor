#!/usr/bin/env ruby

require_relative '../config/bootstrap.rb'
require 'rufus-scheduler'
require 'routemaster/services/monitor'

scheduler = Rufus::Scheduler.new
monitor = Routemaster::Services::Monitor.new

scheduler.every('5s') { monitor.call }

scheduler.join
